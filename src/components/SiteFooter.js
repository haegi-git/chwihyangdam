export default function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-line/80 bg-paper-deep/50">
      <div className="mx-auto flex w-full max-w-5xl flex-col gap-1 px-5 py-8 text-sm text-ink-soft sm:flex-row sm:items-center sm:justify-between">
        <p className="font-serif text-ink">취향담</p>
        <p>조용한 취향을 위한 작은 자리. 일기는 기본적으로 비공개입니다.</p>
      </div>
    </footer>
  );
}
