export default function SiteFooter() {
  return (
    <footer className="relative mt-auto border-t border-line/60 bg-paper-deep/30">
      <div className="mx-auto flex w-full max-w-5xl flex-col gap-2 px-6 py-10 text-sm leading-6 text-ink-soft sm:flex-row sm:items-center sm:justify-between md:px-8">
        <p className="font-serif text-base text-ink">취향담</p>
        <p>조용한 취향을 위한 작은 자리. 일기는 기본적으로 비공개입니다.</p>
      </div>
    </footer>
  );
}
